# Gazilla server config file. You can edit these values to change how the server behaves.

# General server configuration
server:
  # Update rate in Hz
  updateRate: 60

  # Full path of track to load and simulate
  trackFile: "assets/tracks/FSG_trackdrive.yaml"

# Networking configuration
network:
  # Server communication port
  port: 41803

rosbridge:
  # If the ROS bridge is enabled or not
  enabled: true

  # ROS port
  port: 41802